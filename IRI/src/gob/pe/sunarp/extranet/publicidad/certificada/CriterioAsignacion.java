package gob.pe.sunarp.extranet.publicidad.certificada;

import com.jcorporate.expresso.core.db.DBConnection;
import gob.pe.sunarp.extranet.dbobj.DboCriteriosAsigna;
import java.sql.Connection;

public class CriterioAsignacion {
private Connection conn;

protected void grabarDBCriterioAsigna(){
	
	/*
	DBConnection dconn = new DBConnection(conn);
	
	DboCriteriosAsigna dboCritAsigna = new DboCriteriosAsigna();
	dboCritAsigna.setField(dboCritAsigna.CAMPO_CUENTA_ID, );
	dboCritAsigna.setField(dboCritAsigna.CAMPO_REG_PUB_ID, );
	dboCritAsigna.setField(dboCritAsigna.CAMPO_OFIC_REG_ID, );
	dboCritAsigna.setField(dboCritAsigna.CAMPO_CERTIFICADO_ID, );
	dboCritAsigna.setField(dboCritAsigna.CAMPO_GRUPO_ID, );
	dboCritAsigna.setField(dboCritAsigna.CAMPO_ESTADO, );
	*/

}

}

